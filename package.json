{
  "name": "http-webhook",
  "version": "1.0.0",
  "description": "Simple Express webhook receiver that records incoming requests and displays them on an admin dashboard.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^5.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
